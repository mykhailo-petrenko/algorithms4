# Union Find

[Union find JS](uf.js)

```javascript
const uf = new UF(NODES_COUNT);

uf.add(a, b);

uf.connected(a, b); // is connected ?

uf.find(a); // get group id

uf.groupsCount(); // get amount of groups 
```
